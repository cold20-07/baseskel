# Railway Environment Configuration
# This file is automatically loaded in Railway deployments

# Host Configuration - Allow all hosts for Railway
ALLOWED_HOSTS=*

# Environment
ENVIRONMENT=production

# Port Configuration
PORT=8080

# CORS Configuration
CORS_ORIGINS=https://base-skel.netlify.app,http://localhost:3000

# Railway Detection
RAILWAY_ENVIRONMENT_NAME=production

# Supabase Configuration (set these in Railway dashboard)
# SUPABASE_URL=your_supabase_url_here
# SUPABASE_KEY=your_supabase_key_here
